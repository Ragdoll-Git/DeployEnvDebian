services:
  brainbash-dev:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/app
      - ollama_data:/root/.ollama
    command: tail -f /dev/null

volumes:
  ollama_data:
